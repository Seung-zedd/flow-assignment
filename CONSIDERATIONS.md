# 고려사항 판단 기록

요구사항 원문이 제시한 19개 항목과, 그 목록에 없어 직접 찾아낸 9개 항목(E1~E9)을 합쳐 **28항목**을 다룹니다. 항목마다 네 가지를 적었습니다 — **판단**(구현 / 부분 구현 / 보류 / 스코프 밖), **무엇을 했나**(파일·함수 근거), **왜**(대안을 무엇 때문에 접었는지), **한계와 남은 위험**.

각 항목의 요약 판정은 `.moai/specs/SPEC-UPLOAD-001/plan.md` §9 매트릭스에 표로 있고, 이 문서는 그 표를 서술로 편 것입니다. 판정이 어긋나면 매트릭스가 아니라 이 문서가 최신입니다.

---

## 3-1. 검증 · 보안 관점

### 3-1-a. 확장자만 믿어도 되는가 — 파일 내용 · 시그니처(매직 넘버) 불일치

**판단: 구현**

파일명 확장자 검사에 더해 파일 내용을 실제로 들여다봅니다. `src/lib/server/upload/signature.ts`의 `sniffSignature()`가 두 가지를 봅니다 — 이진 포맷은 `file-type` 라이브러리로 매직 넘버를 읽고, 텍스트 실행 파일은 선행 바이트를 직접 스니핑합니다(`#!/bin/sh` 셔뱅, `<!DOCTYPE html`, `MZ` PE 헤더 등). 판별된 타입은 별칭 정규화를 거쳐 **차단 목록과 대조**되고, 목록에 있으면 `SIGNATURE_BLOCKED`(415)로 거부합니다. 판정 진입점은 `src/lib/server/upload/decide.ts`의 `decideUpload()` 하나입니다.

**왜.** `report.jpg`라는 이름의 실행 파일은 확장자 검사만으로는 통과합니다. 확장자는 사용자가 자유롭게 바꿀 수 있는 문자열이라 그 자체로는 아무것도 보증하지 않습니다. 다만 내용 판별을 **거부의 근거**로 쓰는 방식은 두 가지로 나뉘는데, 우리는 "판별 결과가 차단 목록에 있을 때만 거부"를 택했습니다. "선언 확장자와 판별 결과가 다르면 거부"라는 더 강한 대안은 3-1-h에서 따로 다룹니다.

**한계와 남은 위험.** 순수 텍스트 파일은 원리적으로 판별할 수 없습니다. 매직 넘버가 없고 선행 바이트도 평범한 텍스트라면 `sniffSignature()`는 판별 불가로 통과시킵니다(`signature.ts`의 `@MX:WARN`에 그 판단을 명시해 두었습니다). 시그니처 검사는 확장자 정책을 **보강**할 뿐 대체하지 못합니다.

또 하나, 판정 순서가 결과에 영향을 줍니다. `decideUpload()`는 확장자 대조를 시그니처 대조보다 **항상 먼저** 실행합니다. 그래서 `html`을 차단한 뒤 문자 그대로 `page.html`을 올리면 확장자 단계에서 이미 걸려 `BLOCKED_EXTENSION`이 나오고 시그니처 검사에 도달하지 않습니다. 인수 기준 AC-UPLOAD-014의 2절은 이 경우를 `SIGNATURE_BLOCKED`로 적고 있는데, 실제 코드 경로와 다릅니다. **코드를 그대로 두고 acceptance.md 문구를 `BLOCKED_EXTENSION`으로 정정하기로 판정**했습니다(sync 단계에서 반영). 판정 순서를 뒤집으면 M1에서 이미 감사·고정한 요구사항 순서(REQ-008이 REQ-009보다 앞)를 건드리게 되어 회귀 위험이 더 컸습니다. AC의 원래 취지 — HTML 내용도 시그니처로 잡혀야 한다 — 는 확장자가 `html`이 아닌 위장 파일(`notes.dat`)로 별도 검증해 `SIGNATURE_BLOCKED` 경로가 실제로 살아 있음을 확인했습니다.

### 3-1-b. 대소문자 · 이중 확장자 · `.tar.gz`

**판단: 구현**

`src/lib/server/upload/extension.ts`의 `extractExtensionSegments()`가 파일명을 소문자화한 뒤 `.`로 쪼개고, 첫 세그먼트(본체)를 제외한 **모든 dot-segment**를 확장자 후보로 뽑습니다. `file.exe.txt`는 `['exe', 'txt']`가 되어 `exe`가 차단돼 있으면 걸립니다. `file.PDF`는 소문자화로 `pdf`가 되고, `archive.tar.gz`는 `['tar', 'gz']` 둘 다 검사 대상입니다. 후보는 `canonicalizeExtension()`으로 대표형까지 접습니다(`jpeg` → `jpg`, `htm` → `html`, `yml` → `yaml` 등).

**왜.** 마지막 세그먼트만 보는 흔한 구현은 `malware.exe.txt`를 그대로 통과시킵니다. Windows가 알려진 확장자를 숨기는 설정에서는 사용자에게 `malware.exe`로 보이기까지 합니다. 전 세그먼트 검사는 오탐(`my.report.2026.pdf`의 `report`·`2026`이 후보가 됨)을 늘리지만, 후보가 차단 목록에 **있을 때만** 거부하므로 실제 오탐으로 이어지려면 관리자가 `report`를 직접 차단해야 합니다. 그 경우는 관리자의 의도로 보는 게 맞습니다.

**한계와 남은 위험.** 별칭 표(`EXTENSION_ALIASES`)는 손으로 관리하는 5쌍짜리 목록이라 완전하지 않습니다. 표를 늘릴 때 정책 저장 경로와 업로드 판정 경로가 갈라지지 않도록 표 자체를 단일 원본으로 두고 `@MX:ANCHOR`를 붙였습니다.

### 3-1-c. 확장자 없는 파일 · 점으로 시작하는 파일(`.env`) · 매우 긴 파일명

**판단: 구현 (fail-closed)**

`extension.ts`의 `normalizeFilename()`이 파일명을 NFC 정규화 → 제어문자 제거 → 경로 구분자(`/`, `\`) 제거 → `..` 시퀀스 제거 → 공백 정리 → **255바이트 절단** 순으로 다듬습니다. 절단은 `truncateUtf8()`이 맡아 코드 포인트를 반으로 쪼개지 않습니다.

- **확장자 없는 파일**(`README`): 후보가 하나도 없으면 `NO_EXTENSION`(415)으로 **거부**합니다.
- **dotfile**(`.env`): 첫 세그먼트는 빈 문자열이므로 `env`가 후보로 잡힙니다. 커스텀에 `env`를 추가해 두면 그대로 차단됩니다.
- **매우 긴 파일명**: 255바이트를 넘으면 잘립니다.

**왜.** 확장자가 없으면 확장자 기반 정책을 적용할 근거 자체가 없습니다. 이때 통과시키면 "확장자를 지우면 정책을 우회할 수 있다"는 구멍이 생기므로 **모르는 것은 거부**(fail-closed)로 정했습니다.

**한계와 남은 위험 — 긴 파일명의 fail-closed는 의도된 결과입니다.** 절단은 뒤에서 자르는 것이 아니라 앞에서 담을 수 있는 만큼만 담는 방식이라, `a`가 300자 이어진 뒤의 `.txt`는 255바이트 경계 밖으로 밀려나 **사라집니다**. 남는 것은 확장자 후보가 없는 254자짜리 이름뿐이고, 결과는 `NO_EXTENSION`(415) 거부입니다. 이 동작을 그대로 유지하기로 판정했습니다 — 절단 방향을 "확장자를 보존하도록" 바꾸면 공격자가 긴 이름으로 앞부분을 채워 확장자 판정을 조작할 여지가 생기고, 무엇보다 이미 감사가 끝난 `normalizeFilename()`을 건드려야 합니다. 절단 후에도 확장자가 살아남는 긴 파일명은 정상 업로드되며, 그 경로도 별도 테스트로 확인했습니다.

**대안으로 검토했다 접은 것 — 화이트리스트 모델.** 허용 확장자를 열거하는 방식으로 전환하면 "모르는 것은 거부"가 정책의 기본값이 되어 확장자 없는 파일도 자연스럽게 걸러지고, 대신 `README`·`Dockerfile` 같은 평범한 파일을 하나씩 허용하는 판단으로 성격이 바뀝니다. 요구사항이 "차단할 확장자를 관리하는 화면"을 전제하므로 블랙리스트를 유지하는 한 이 결정은 뒤집히지 않습니다(3-4-c 참고).

### 3-1-d. 확장자 입력값 검증 · 정규화

**판단: 구현**

`extension.ts`의 `normalizeExtensionInput()`이 **NFKC 정규화 → 앞뒤 공백 제거 → 앞뒤 점 제거 → 소문자화 → 별칭 대표형 변환** 순서로 처리한 뒤 `^[a-z0-9]{1,20}$`를 강제합니다. 실패하면 `EXT_EMPTY`(400) · `EXT_TOO_LONG`(400) · `EXT_INVALID_CHARS`(400) 중 하나로 거부합니다. `.EXE`, ` exe `, `exe.`은 모두 `exe`로 접히고, `ｅｘｅ`(전각)는 NFKC로 `exe`가 됩니다. DB에도 같은 규칙이 `CHECK (extension ~ '^[a-z0-9]{1,20}$')` 제약으로 이중으로 걸려 있습니다.

**왜.** 정규화하지 않으면 `exe`와 `EXE`와 `.exe`가 세 개의 다른 행이 되어, 관리자는 차단했다고 믿지만 실제로는 한 표기만 막히는 상태가 됩니다. 전각 문자는 특히 위험합니다 — 눈으로는 구별되지 않는데 바이트가 다릅니다.

**한계와 남은 위험.** **파일명에는 NFC, 확장자 입력에는 NFKC**를 쓰며 이 둘을 섞지 않습니다. 파일명에 NFKC를 적용하면 전각 문자가 접혀 사용자가 올린 이름과 기록된 이름이 달라지기 때문입니다. 두 정규화 형식이 다르다는 사실 자체가 나중에 코드를 읽는 사람에게는 실수하기 쉬운 지점이라 주석으로 명시해 두었습니다.

### 3-1-e. 서버 사이드 검증의 필요성

**판단: 구현**

**모든 판정은 서버에서만 이뤄집니다.** 유일한 강제 지점은 `src/routes/api/upload/+server.ts`의 `POST` 핸들러이고, 그 안에서 `decideUpload()` 한 번이 판정을 끝냅니다. 브라우저가 보여 주는 차단 안내(`UploadArea.svelte`의 `isClientHintBlocked`)는 **업로드를 막지 않습니다** — 사용자는 힌트를 무시하고 그대로 전송할 수 있고, 서버가 동일하게 판정합니다. 그 힌트가 편의 기능일 뿐임을 알리는 고정 문구(`CLIENT_HINT_DISCLAIMER`)도 화면에 함께 띄웁니다.

**왜.** 클라이언트 검증은 개발자 도구·`curl`·스크립트로 전부 우회됩니다. 그러니 클라이언트 검증을 "검증"이라 부르면 안 되고, 사용자가 4MB 파일을 다 올린 뒤에야 거부당하는 일을 줄여 주는 **편의 기능**으로 위치를 명확히 하는 편이 낫다고 봤습니다.

**검증 방식.** 인수 기준(AC-UPLOAD-008 ~ 015)은 전부 **엔드포인트를 직접 호출**해 검증합니다. 클라이언트를 거치지 않는 이 경로가 곧 우회 시나리오의 재현이기도 합니다. 배포 후에는 실행 중인 인스턴스를 대상으로 Playwright 스모크 런을 **한 번** 돌려 화면 문구와 실제 거부 동작을 눈으로 확인했습니다(`e2e/`). CI에 E2E를 배선하지는 않았습니다 — 1회성 확인이 주는 확신과 CI 유지 비용을 견줬을 때 이 규모에서는 배선 비용이 더 컸습니다.

**한계와 남은 위험.** 클라이언트 힌트는 `decideUpload()`를 재사용하지 못합니다. `$lib/server/**`는 SvelteKit 경계상 클라이언트 컴포넌트에서 임포트할 수 없기 때문입니다. 대신 `+page.server.ts`의 `load()`가 차단 목록과 별칭 표를 데이터로 내려주고 컴포넌트가 경량 함수로 대조합니다. 힌트 로직과 서버 판정 로직이 물리적으로 갈라져 있으므로, 판정 규칙이 바뀌면 양쪽이 어긋날 수 있습니다. 다만 **어긋나도 보안 결과는 바뀌지 않습니다** — 힌트가 틀리면 UX가 나빠질 뿐 서버 판정은 그대로입니다.

이 항목과 이어지는 테스트 격리 문제는 E5에 적었습니다.

### 3-1-f. 파일 크기 제한 · 요청당 파일 개수

**판단: 구현**

**요청 하나에 파일 하나**입니다. 따라서 4MB(`constants.ts`의 `MAX_UPLOAD_BYTES`) 상한은 파일 상한이자 요청 상한이며, 두 겹으로 강제합니다.

1. `Content-Length` 헤더가 있고 4MB를 넘으면 **본문을 읽기 전에** `FILE_TOO_LARGE`(413)로 거부합니다.
2. 헤더가 없거나(청크 전송) 실제보다 작게 신고한 경우에도, 파싱 후 실측 `file.size`로 다시 확인해 같은 코드로 거부합니다.

여러 파일을 고른 경우 클라이언트(`UploadArea.svelte`)가 **순차적으로** 요청을 나눠 보냅니다.

**왜.** 4MB는 Vercel Function의 요청 본문 한도 4.5MB에서 역산한 값입니다. 플랫폼 한도에 딱 붙이면 멀티파트 오버헤드 때문에 애플리케이션이 판정하기도 전에 플랫폼이 먼저 끊어, 사용자는 사유 없는 실패를 보게 됩니다. 여유를 두어야 우리가 만든 문구로 거부할 수 있습니다. 플랫폼 한도는 최후 방어선이지 판정 근거가 아닙니다.

**한계와 남은 위험.** "본문을 바이트 상한을 건 채로 스트리밍하며 읽는다"는 원래 계획은 문자 그대로 구현하지 못했습니다. 멀티파트 파싱이 SvelteKit·undici 내부에서 일어나 핸들러 코드에 스트리밍 상한을 걸 지점이 없습니다. 실질적인 메모리 방어선은 플랫폼의 4.5MB 한도이며, `file.size`가 판정 근거라는 계약은 지켰습니다.

### 3-1-g. 원본 파일명을 저장 키로 쓰는 위험

**판단: 구현**

저장 키는 `uploads/{UUID}` 형태이고 **확장자를 포함하지 않습니다**(`src/routes/api/upload/+server.ts`, `crypto.randomUUID()`). 원본 파일명은 `normalizeFilename()`을 거친 값만 `upload_attempt.original_name`에 메타데이터로 남기며, 클라이언트가 보낸 바이트를 그대로 저장하지 않습니다.

**왜.** 원본 파일명을 키로 쓰면 세 가지가 한꺼번에 열립니다 — 경로 조작(`../../etc/passwd`), 저장 위치에 따른 실행 가능성, 그리고 확장자 기반 `Content-Type` 추론. 세 번째가 특히 조용합니다. 키에 `.html`이 남아 있으면 나중에 누군가 그 객체를 서빙하는 코드를 붙였을 때 저장형 XSS가 됩니다. 키에서 확장자를 아예 없애면 그 사고 경로가 구조적으로 닫힙니다.

**한계와 남은 위험.** 표시·감사용으로 남기는 정규화 파일명에는 여전히 사용자 입력이 들어 있습니다. 이 값을 화면에 렌더링할 때는 Svelte의 기본 이스케이프에 의존하고 있으며, 앞으로 `{@html}`로 렌더링하는 코드가 붙으면 위험해집니다.

### 3-1-h. MIME 타입 스푸핑

**판단: 구현 (기록만, 거부하지 않음)**

브라우저가 신고한 MIME(`declared_mime`)은 **판정에 전혀 쓰지 않고 기록만** 합니다. 파일 내용에서 판별한 타입(`detected_mime`)과 선언 확장자가 다르면 응답의 `mismatch` 플래그와 `upload_attempt` 행, 구조화 로그에 남기지만 **그 사실만으로는 거부하지 않습니다**.

**왜.** 불일치를 곧바로 거부하는 대안을 진지하게 검토했고 접었습니다. 오탐이 너무 많기 때문입니다. 확장자와 내용이 정당하게 어긋나는 경우가 흔합니다 — 텍스트 기반 포맷은 매직 넘버가 없어 판별이 자주 실패하고, 컨테이너 포맷(`docx`, `xlsx`, `jar`)은 전부 zip으로 잡히며, 브라우저마다 같은 파일에 다른 MIME을 붙입니다. 정상 파일이 반복해서 막히면 사용자는 차단 메시지 전체를 신뢰하지 않게 되고, 그때는 **진짜 차단도 무시**됩니다. 오탐이 차단 UX의 신뢰도를 깎는 비용이 불일치 자체의 위험보다 크다고 판단했습니다.

**한계와 남은 위험.** 차단 목록에 없는 형식으로 위장한 파일은 통과합니다. 예를 들어 `exe`가 차단돼 있지 않으면 PE 실행 파일을 `photo.jpg`로 올려도 막히지 않습니다. 이는 블랙리스트 모델의 구조적 한계이며, `mismatch` 기록이 사후 조사의 단서로 남습니다.

---

## 3-2. 정책 · 데이터 관점

### 3-2-a. 고정 확장자와 커스텀 확장자가 겹칠 때

**판단: 구현**

커스텀에 이미 고정으로 존재하는 확장자(예: `exe`)를 추가하려 하면 `EXT_IS_FIXED`(409)로 **거부**하고, "고정 확장자예요. 위 체크박스에서 관리해 주세요."라고 안내합니다. 두 종류를 `kind` 컬럼으로 구분해 **한 테이블에** 담았기 때문에 `UNIQUE(extension)` 제약 하나가 이 규칙을 DB 차원에서 뒷받침합니다(`policy-repo.ts`의 `addCustom()`, `isUniqueViolation()`).

**왜.** 조용히 받아들이고 화면에서 숨기는 방식(accept-and-hide)을 검토했다가 접었습니다. 같은 확장자에 두 개의 진실이 생기면, 관리자가 고정 체크박스를 해제했을 때 커스텀 쪽이 살아 있어 여전히 막히는 상황이 생깁니다. 그때 관리자는 자기가 해제한 것이 왜 안 풀리는지 알 방법이 없습니다. 테이블을 둘로 나누는 설계도 같은 이유로 접었습니다 — 나누면 겹침 여부를 애플리케이션 코드가 매번 확인해야 하고, 한 곳만 빠뜨려도 중복이 들어옵니다.

**한계와 남은 위험.** 없음. 애플리케이션 검사와 DB 제약이 이중으로 걸려 있어, 코드 경로가 새로 생겨도 제약이 마지막 방어선이 됩니다.

### 3-2-b. 정책 변경 이력 · 감사

**판단: 보류**

`policy_audit_log` 테이블을 만들지 않았습니다. 대신 정책 변경 시 구조화 로그를 남기고, 향후 확장 지점으로 DDL 초안만 문서에 남깁니다.

**왜.** 이 애플리케이션에는 인증이 없습니다. 감사 로그의 핵심 질문은 "**누가** 언제 무엇을 바꿨는가"인데, 인증 없이는 "누가"를 채울 수 없어 절반만 참인 기록이 됩니다. 절반짜리 감사 로그는 없는 것보다 나쁠 수 있습니다 — 있다는 사실 자체가 추적 가능하다는 착각을 만듭니다. 인증을 먼저 도입한 뒤에 붙이는 것이 순서라고 봤습니다.

**한계와 남은 위험.** 지금은 누가 정책을 바꿨는지 알 수 없고, 되돌릴 근거도 남지 않습니다. 무인증 공개 배포에서는 누구나 정책을 바꿀 수 있으므로 실운영이라면 인증이 먼저입니다(3-4-c).

### 3-2-c. 200개 / 20자 제한의 근거와 초과 시 UX

**판단: 구현**

두 숫자는 요구사항이 명시한 값이고, 코드에서는 `constants.ts`의 `MAX_CUSTOM_EXTENSIONS = 200`·`MAX_EXTENSION_LENGTH = 20` 상수 하나씩으로 관리합니다. 초과 시에는 전용 사유 코드로 응답합니다 — `EXT_LIMIT_REACHED`(409, "커스텀 확장자는 최대 200개까지 추가할 수 있어요.") · `EXT_TOO_LONG`(400, "확장자는 최대 20자까지 입력할 수 있어요."). 화면에는 항상 `N/200` 카운터를 띄워 상한에 다가가는 것이 미리 보이게 했습니다.

**왜.** 상한 검사는 카운트와 삽입을 **하나의 원자 SQL**(조건부 INSERT를 담은 CTE)로 묶었습니다(`policy-repo.ts`의 `addCustom()`). 세어 보고 나서 넣는 두 단계로 짜면 동시 요청 두 건이 199개를 함께 읽고 둘 다 넣어 201개가 됩니다.

**한계와 남은 위험.** READ COMMITTED 격리 수준에서는 원자 SQL로도 경합 폭을 좁힐 뿐 완전히 닫지는 못합니다. 이 잔여 위험은 `policy-repo.ts`에 `@MX:WARN`으로 표시해 두었습니다. 완전히 닫으려면 `SERIALIZABLE` 격리나 어드바이저리 락이 필요한데, 단일 관리자 시나리오에서 그 비용은 과합니다.

### 3-2-d. 대량 조회 · 저장 시 성능과 인덱스

**판단: 구현 (추가 인덱스 없음)**

인덱스는 `PRIMARY KEY (id)`와 `UNIQUE (extension)` 둘뿐입니다. 정책 조회는 `getPolicy()`가 **한 번의 SELECT**로 고정·커스텀을 함께 읽고 코드에서 가릅니다.

**왜.** 이 테이블의 행 수 상한은 207개(고정 7 + 커스텀 200)입니다. 이 크기에서 PostgreSQL은 어차피 순차 스캔을 고르며, 인덱스를 더 만들어도 조회는 빨라지지 않고 쓰기 비용만 늘어납니다. 조회를 두 번으로 나누는 대안도 접었습니다 — Neon HTTP 드라이버는 쿼리 한 건이 왕복 한 번이라, 분리하면 정책을 읽을 때마다 왕복이 두 배가 됩니다. `getPolicy()`는 조회는 물론 토글·추가·삭제 응답에서도 매번 불리는 가장 잦은 읽기 경로입니다.

**한계와 남은 위험.** 상한이 크게 늘거나 사용자별 정책이 생겨 행 수가 수만 개가 되면 이 판단은 뒤집힙니다. 그때는 `kind` 부분 인덱스와 페이지네이션이 필요합니다.

---

## 3-3. UX · 예외 관점

### 3-3-a. 차단 시 보여 줄 메시지 — 무엇이, 왜 막혔는가

**판단: 구현**

사유 코드 10종 각각에 HTTP 상태와 사용자 문구를 붙여 `src/lib/server/upload/reason-codes.ts` 한 곳에서 관리합니다. 거부 응답은 형태가 항상 같습니다.

```json
{ "ok": false, "error": { "code": "BLOCKED_EXTENSION", "message": "차단된 확장자예요: exe", "details": { "matched": "exe" } } }
```

**무엇이** 막혔는지는 `details.matched`에 **실제로 걸린 대표형 세그먼트**를 담아 알려 줍니다. `jpg`를 차단한 상태에서 `photo.jpeg`를 올리면 `matched: "jpg"`가 돌아옵니다 — 사용자가 올린 것은 `jpeg`인데 차단 목록에 있는 것은 `jpg`이므로, 이 정보가 없으면 왜 막혔는지 알 수 없습니다. 시그니처로 막힌 경우는 `details.detected`에 판별된 형식을 담습니다.

**왜.** "업로드할 수 없는 파일입니다" 같은 문구는 사용자가 다음에 무엇을 해야 할지 알려 주지 않습니다. 사유 코드를 코드·상태·문구 세 쌍으로 한 표에 묶어 둔 것은, 새 사유가 생겼을 때 어느 한 곳만 고치고 나머지를 빠뜨리는 일을 막기 위해서입니다.

**한계와 남은 위험.** 문구가 한국어로 고정돼 있고 다국어 처리가 없습니다.

### 3-3-b. 로딩 · 에러 · 네트워크 실패 상태

**판단: 구현**

정책 변경·업로드 요청 중에는 해당 버튼·체크박스가 pending 상태로 잠기고, 실패하면 낙관적 갱신을 되돌린 뒤 서버가 준 메시지를 그대로 화면에 띄웁니다. 업로드는 여러 파일을 골라도 순차로 전송하며, 각 파일의 진행·결과를 개별로 보여 줍니다.

**왜.** 잠그지 않으면 사용자가 응답을 기다리는 동안 같은 버튼을 여러 번 눌러 중복 요청이 나갑니다. 정책 토글은 멱등이라 결과는 같지만, 커스텀 추가는 두 번째 요청이 `EXT_DUPLICATE`로 실패하며 사용자에게는 방금 성공한 동작이 실패한 것처럼 보입니다.

**한계와 남은 위험.** 재시도 버튼이 없어 네트워크 실패 시 사용자가 직접 다시 시도해야 합니다. 요청 타임아웃도 별도로 걸지 않아 브라우저 기본값을 따릅니다.

### 3-3-c. 저장 실패 시 화면 상태와 DB 상태의 일관성

**판단: 구현**

정책 변경 엔드포인트는 변경 후의 **정식 정책 상태**를 응답에 함께 돌려줍니다(`PATCH /api/policy/fixed/[ext]`는 `{ ok: true, fixed: [...] }`). 화면은 클릭 즉시 낙관적으로 상태를 바꾸고, 실패하면 **서버가 준 정식 상태로 되돌립니다**(`FixedExtensionList.svelte`). 되돌리는 값이 "직전 로컬 상태"가 아니라 서버가 방금 알려 준 값이라는 점이 핵심입니다.

**왜.** 직전 로컬 상태로 되돌리는 흔한 구현은, 그 사이 다른 탭이나 다른 사람이 정책을 바꿨을 경우 화면을 **더 낡은 값**으로 되돌립니다. 서버가 진실이므로 서버가 준 값으로 재조정해야 화면과 DB가 반드시 수렴합니다. 이 롤백 과도 상태는 jsdom 컴포넌트 테스트로 직접 검증했습니다(AC-UPLOAD-016a).

**한계와 남은 위험.** 실시간 동기화는 없습니다. 다른 탭에서 바뀐 정책은 새로고침하거나 다음 변경 요청의 응답을 받아야 반영됩니다.

### 3-3-d. 접근성 · 반응형

**판단: 부분 구현**

시맨틱 마크업, 폼 요소에 연결된 라벨, 키보드 조작(체크박스·입력·버튼 전부 탭 이동과 엔터로 조작 가능), 포커스 표시까지 챙겼습니다. 상태 변화 메시지는 화면에 텍스트로 남습니다.

**왜 부분인가.** 폭넓은 반응형 레이아웃(모바일 전용 레이아웃, 브레이크포인트별 조정)과 스크린리더 전용 라이브 리전 검증은 보류했습니다. 요구사항에서 선택 항목으로 표시된 영역이고, 남은 시간을 서버 사이드 검증의 정확성에 쓰는 편이 이 프로젝트의 중심축과 더 맞다고 판단했습니다.

**한계와 남은 위험.** 좁은 화면에서 커스텀 확장자 태그 목록이 넘칠 수 있습니다. 스크린리더로 실제 낭독 흐름을 검증하지 않았으므로 WCAG 준수를 주장하지 않습니다.

---

## 3-4. 운영 관점

### 3-4-a. 새로고침 · 동시 편집 시 정합성

**판단: 구현**

**서버가 진실입니다.** 초기 정책 상태는 `+page.server.ts`의 `load()`가 서버에서 렌더링해 내려주므로 새로고침하면 항상 DB의 현재 값이 보입니다. 동시 편집은 last-write-wins로 처리하고, 그로 인한 데이터 파손은 `UNIQUE(extension)` 제약이 막습니다. 고정 확장자 토글은 멱등이라 같은 값으로 여러 번 호출해도 결과가 같습니다.

**왜.** 낙관적 락(버전 컬럼)이나 비관적 락을 검토했다가 접었습니다. 이 정책은 관리자가 가끔 바꾸는 전역 설정이고 충돌 자체가 드뭅니다. 충돌이 나도 잃는 것은 "누가 먼저 눌렀는가"뿐이며, 잘못된 상태가 남지는 않습니다. 락을 도입하면 사용자에게 "다른 사람이 먼저 바꿨습니다, 새로고침하세요" 같은 대화를 강요하게 되는데, 그 비용이 얻는 것보다 큽니다.

**한계와 남은 위험.** 두 관리자가 동시에 반대 방향으로 토글하면 나중 요청이 이깁니다. 3-2-b(감사 로그 부재)와 겹쳐서, 누가 마지막으로 바꿨는지 알 방법이 없습니다.

### 3-4-b. 로그 · 모니터링 관점에서 무엇을 남길 것인가

**판단: 구현**

파일 단위 판정(수락·거부) 하나마다 **`upload_attempt` 테이블에 정확히 1행 + 구조화 로그 1줄**을 남깁니다(`src/routes/api/upload/+server.ts`의 `logAttempt()`·`recordAndLogAttempt()`, `db/upload-repo.ts`의 `recordUploadAttempt()`). 남기는 것은 정규화 파일명 · 확장자 · 선언 MIME · 판별 MIME · 크기 · 결과 · 사유 코드 · 저장 키 · 시각, 그리고 요청 추적용 `x-vercel-id`입니다.

**파일 내용은 어느 쪽에도 기록하지 않습니다.** 로그의 파일명은 64바이트로 절단합니다.

본문을 읽기 전에 확정되는 **요청 단위 거부**(`Content-Length` 선차단으로 나는 413)는 테이블에 행을 남기지 않고 구조화 로그만 남깁니다. 그 시점에는 파일명과 실제 크기가 확정되지 않아 `original_name`·`size_bytes`가 `NOT NULL`인 행을 만들 수 없기 때문입니다.

**왜.** 거부만 남기면 "정책이 과하게 막고 있는가"를 판단할 분모가 없습니다. 수락도 함께 남겨야 거부율을 볼 수 있습니다. 파일 내용을 남기지 않는 것은 타협 없는 원칙입니다 — 로그와 DB는 백업·복제·모니터링 도구로 계속 퍼지고, 그 어디에도 사용자 파일이 있어서는 안 됩니다.

**한계와 남은 위험.** 알림·대시보드는 없습니다. 거부율이 급등해도 자동으로 알아차릴 방법이 없고, 로그를 직접 조회해야 합니다. `upload_attempt`에 보존 기간 정책이 없어 무한히 쌓입니다.

### 3-4-c. 향후 확장 — 사용자별 정책, 화이트리스트 전환

**판단: 보류 (뒤집힐 판단을 명시)**

지금은 전역 단일 정책이고 블랙리스트 모델입니다. 확장 시 무엇이 바뀌는지만 명확히 적어 둡니다.

- **사용자별 정책**: `blocked_extension`에 `owner_id`를 더하고 `UNIQUE(extension)`을 `UNIQUE(owner_id, extension)`으로 바꾸면 구조는 그대로 확장됩니다. 다만 **인증이 먼저**입니다(3-2-b). 인증이 들어오면 감사 로그도 함께 의미를 갖게 됩니다.
- **화이트리스트 전환**: 이 전환은 정책의 기본값을 뒤집습니다. 지금은 "목록에 없으면 통과"인데 화이트리스트에서는 "목록에 없으면 거부"가 됩니다. 그러면 3-1-c의 `NO_EXTENSION` 거부는 특별 규칙이 아니라 일반 규칙의 한 사례가 되고, 3-1-h의 "불일치를 거부하지 않는다"는 판단도 재검토 대상이 됩니다 — 허용 목록이 있으면 판별 결과가 허용 목록에 없을 때 거부하는 것이 자연스러워지기 때문입니다.

**왜 지금은 아닌가.** "차단할 확장자를 관리하는 화면"이 전제이므로 블랙리스트가 요구사항입니다. 화이트리스트는 더 안전하지만 다른 제품입니다.

**한계와 남은 위험.** 블랙리스트인 한 "목록에 없는 새로운 위험 확장자"는 계속 통과합니다. 이것은 구현 결함이 아니라 모델의 성질입니다.

---

## E. 요구사항 목록에 없어 직접 찾아낸 항목

### E1. 업로드 파일을 다시 내려줄 때의 저장형 XSS

**판단: 스코프 밖 (위험 표면 제거)**

업로드된 파일을 다시 내려주는 다운로드·미리보기 엔드포인트를 **만들지 않았습니다.** Blob 스토어는 `access: 'private'`으로 두어 URL만으로는 열 수 없습니다(`src/lib/server/blob/store.ts`의 `createVercelBlobStore()`).

**왜.** 재제공하는 순간 `Content-Disposition`·`Content-Type` 처리 실수 하나로 저장형 XSS가 열립니다 — 업로드된 `.svg`나 `.html`이 **우리 오리진에서** 실행되면 세션과 쿠키가 그대로 노출되고, 피싱 페이지 호스팅에도 쓰입니다. 요구 범위는 "정상 파일은 업로드 성공 처리"까지이므로, 요구되지 않은 기능 때문에 위험 표면을 여는 대신 설계 단계에서 제거했습니다.

**한계와 남은 위험.** 업로드한 파일을 사용자가 확인할 방법이 없습니다. 나중에 재제공이 필요해지면 별도 오리진(샌드박스 도메인) + `Content-Disposition: attachment` + `Content-Type: application/octet-stream` 고정이 최소 조건입니다.

### E2. 압축파일 내부 엔트리 · zip-slip · 압축 폭탄

**판단: 스코프 밖 (한계 명시)**

zip/tar 내부 엔트리 검사, zip-slip 대응, 압축 폭탄 방어를 하지 않습니다. **확장자 정책은 컨테이너만 봅니다.**

**왜.** 내부 검사는 별도의 해제·격리 인프라를 요구합니다 — 해제 자체가 디스크와 메모리를 소모하는 공격 표면이고(압축 폭탄), 해제된 파일을 어디에 두고 언제 지울지도 설계해야 합니다. 이 프로젝트의 규모를 넘어섭니다.

**한계와 남은 위험 — 이 한계는 실제 우회 경로입니다.** `exe`를 차단해도 `payload.zip` 안에 `malware.exe`를 넣으면 그대로 통과합니다. 확장자 기반 차단이 막는 것은 "실수로 올린 실행 파일"이지 "의도적으로 숨긴 실행 파일"이 아닙니다. 이 구분을 문서로 남기는 것이 지금 할 수 있는 최선이라고 봤습니다.

### E3. 무인증 공개 배포의 비용 DoS · rate limiting

**판단: 보류**

IP 기반 요청 제한을 만들지 않았습니다.

**왜.** 지금 실질적인 차단기 역할을 하는 것은 세 가지입니다 — 업로드 크기 상한 4MB, 요청당 1파일 제약, 그리고 Vercel Blob Hobby 플랜의 무료 한도. 한도를 넘으면 업로드가 실패할 뿐 요금이 청구되지는 않습니다.

**한계와 남은 위험.** 누구나 반복해서 파일을 올릴 수 있으므로, 스크립트 하나로 저장소 한도를 빠르게 소진시킬 수 있습니다. 그러면 정상 사용자의 업로드도 함께 막힙니다. 개선안은 IP 슬라이딩 윈도우 제한이며, 근본 해법은 인증입니다.

### E4. 정책 캐시를 둘 것인가

**판단: 구현 (= 도입하지 않음)**

정책을 메모리나 외부 캐시에 두지 않고 매 요청 DB에서 읽습니다.

**왜.** 207행 조회는 저렴합니다. 반면 캐시는 **정책 변경이 즉시 반영되지 않는 창**을 만드는데, 이건 성능 문제가 아니라 **보안 문제**입니다. 관리자가 위험 확장자를 급히 차단했는데 TTL이 만료될 때까지 여전히 통과한다면, 정책 화면이 거짓말을 하고 있는 셈입니다. 캐시를 두려면 무효화까지 함께 설계해야 하고, 그 복잡도를 감수할 만한 성능 이득이 이 규모에는 없습니다.

**한계와 남은 위험.** 요청량이 크게 늘면 DB 왕복이 병목이 될 수 있습니다. 그때는 짧은 TTL(수 초)과 정책 변경 시 명시적 무효화를 함께 도입해야 합니다.

### E5. 배포 시 시크릿 취급

**판단: 구현 (zero-trust)**

애플리케이션 코드는 시크릿을 **`$env/dynamic/private`로만** 읽습니다(`db/client.ts`의 `getDb()`, `blob/store.ts`의 `getBlobStore()`). `process.env` 직접 접근은 SvelteKit 밖에서 도는 `scripts/`에만 허용합니다. `PUBLIC_` 접두사를 쓰지 않으므로 클라이언트 번들에 유입될 경로가 없습니다. Vercel 변수는 전부 **Sensitive**로 등록하고, 값은 대화형 프롬프트(`vercel env add NAME production`)에서 직접 붙여 넣습니다 — 값이 명령어 문자열이나 셸 히스토리에 남지 않습니다.

이 규칙은 문서로만 있는 것이 아니라 훅으로 강제됩니다. `.claude/hooks/guards/block-env-edit.mjs`가 `.env` 계열 파일의 읽기·수정을 차단하고, `block-vercel-env-insecure.mjs`가 `--value`·파이프 입력·`--no-sensitive`·`vercel env pull` 같은 위험한 형태를 거부합니다. 근거는 2026년 4월 Vercel 공급망 사고입니다.

**진행 중 실제로 있었던 일 — 솔직히 적습니다.** 2026-08-30 시크릿 준비 과정에서 작성자가 Neon 연결 문자열을 AI 채팅에 **한 번 붙여 넣었습니다.** AI는 그 값의 사용을 거부하고 즉시 교체를 권고했으며, `neondb_owner` 비밀번호를 바로 재설정했습니다. 지금 로컬 `.env`와 Vercel에 들어 있는 값은 전부 **교체 후의 값**입니다. 이 사건은 규칙이 실패한 사례가 아니라 **작동한 사례**로 봅니다 — 노출을 즉시 인지하고, 값을 쓰지 않고, 회전시켰습니다. 어떤 값도 이 문서를 포함해 저장소 어디에도 적지 않았습니다.

**개발 중 발견 — 테스트가 `.env`를 자동으로 읽고 있었습니다.** 로컬에 `.env`를 만든 직후 "환경변수가 없으면 에러를 던진다"를 검증하던 테스트 2건이 깨졌습니다. 원인은 Vite가 프로젝트 루트의 `.env`를 자동으로 읽어 `$env/dynamic/private`에 주입하기 때문이었습니다. 즉 **테스트가 실제 운영 시크릿을 보고 있었고**, 그 상태로는 실제 Neon·Vercel Blob 엔드포인트로 나가는 테스트가 언제든 생길 수 있었습니다. `client.test.ts`·`store.test.ts`에서 `$env/dynamic/private` 모듈 자체를 모킹해, 테스트가 보는 환경 변수를 테스트 파일이 직접 통제하도록 고쳤습니다. 이제 `.env`가 있든 없든 결과가 같습니다. 실수로 발견했지만 그냥 넘길 문제가 아니었습니다.

**한계와 남은 위험.** 시크릿 회전 주기를 정해 두지 않았습니다. Vercel Blob 토큰은 스토어 단위라 세분화된 권한 분리가 불가능합니다.

### E6. 파일명 충돌

**판단: 구현 (구조적으로 불가)**

저장 키가 `uploads/{UUID}`이므로 서로 다른 업로드가 같은 키를 갖는 일이 없습니다. `addRandomSuffix: false`로 두어 Vercel Blob이 키를 임의로 바꾸지도 않습니다.

**왜.** 원본 파일명을 키로 쓰는 설계였다면 같은 이름의 파일이 서로를 덮어썼을 것입니다. UUID를 쓰면 충돌 처리 로직 자체가 필요 없어집니다 — 3-1-g의 보안 결정이 이 문제까지 함께 해결한 셈입니다.

**한계와 남은 위험.** 같은 파일을 두 번 올리면 중복 저장됩니다. 내용 해시 기반 중복 제거는 하지 않았습니다.

### E7. 브라우저 `file.type`은 어디서 오는가

**판단: 구현 (독립 검증 수단이 아님을 명시)**

브라우저가 `File.type`에 넣어 주는 MIME은 **대부분 확장자에서 유추한 파생값**입니다. 파일 내용을 읽고 판별한 결과가 아닙니다. 그래서 이 값은 `declared_mime`으로 기록만 하고 판정에는 쓰지 않습니다.

**왜 항목으로 뽑았나.** 이 사실을 모르면 "확장자와 MIME이 둘 다 `exe`가 아니니 안전하다"는 이중 확인을 했다고 착각하기 쉽습니다. 실제로는 **같은 하나의 출처를 두 번 본 것**이라 독립적인 근거가 되지 않습니다. 3-1-h가 선언 MIME을 판정에서 배제한 진짜 이유가 여기에 있습니다.

**한계와 남은 위험.** 없음 — 값을 신뢰하지 않으므로 틀려도 판정에 영향이 없습니다.

### E8. Blob 저장에 성공한 뒤 DB 기록이 실패하는 경우 (고아 객체)

**판단: 보류 (위험을 감수)**

순서를 `put` → `INSERT`로 고정하고, DB 기록이 실패하면 `orphan_blob` 이벤트를 구조화 로그로 남긴 뒤 오류를 그대로 올립니다(`src/routes/api/upload/+server.ts`). **보상 삭제는 하지 않습니다.**

**왜.** 보상 삭제(DB 실패 시 Blob 객체를 지우는 처리)를 검토하고 접었습니다. **그 삭제도 실패할 수 있기 때문입니다.** 실패하면 결국 같은 고아 객체가 남고, 대신 코드에는 "실패한 보상 처리"를 다시 다뤄야 하는 층이 하나 더 생깁니다. 문제를 해결하는 게 아니라 미루면서 복잡도만 늘리는 셈입니다. 순서를 반대로(`INSERT` → `put`) 두는 대안도 접었습니다 — `blob_pathname`을 미리 확정할 수 없고, 이번엔 "기록은 있는데 파일이 없는" 더 나쁜 불일치가 생깁니다.

**한계와 남은 위험 — 감수한 위험입니다.** 고아 Blob 객체가 남을 수 있습니다. 데모 규모에서 실제 비용은 수 KB 수준이고, 발생하면 로그에 `orphan_blob` 이벤트가 남으므로 사후에 찾아 지울 수 있습니다. 실운영이라면 로그를 읽어 주기적으로 청소하는 배치가 정답입니다.

### E9. 최근 업로드 조회 API의 정보 노출

**판단: 스코프 밖**

최근 업로드 시도를 돌려주는 `GET /api/uploads/recent` 엔드포인트를 **만들지 않았습니다.** `upload_attempt` 테이블은 그대로 두므로 감사 기록 자체는 남으며, 필요하면 DB를 직접 조회합니다.

**왜.** 운영 관점 데모로는 매력적인 기능이지만, **무인증 공개 배포**에서는 다른 사용자가 올린 파일명·선언 MIME·판별 MIME을 누구나 열람하게 됩니다. 파일명에는 개인정보가 자주 들어 있습니다(`이력서_홍길동_2026.pdf`). 얻는 편익보다 노출 비용이 큽니다.

**한계와 남은 위험.** 업로드 이력을 화면에서 확인할 수 없습니다. 인증이 도입되면 자기 이력만 보여 주는 형태로 되살릴 수 있습니다(3-2-b·3-4-c와 같은 전제).

---

## 항목 수 대조

| 구분 | 항목 | 개수 |
|---|---|---|
| 3-1 검증·보안 | a b c d e f g h | 8 |
| 3-2 정책·데이터 | a b c d | 4 |
| 3-3 UX·예외 | a b c d | 4 |
| 3-4 운영 | a b c | 3 |
| E 자체 발굴 | E1 ~ E9 | 9 |
| **합계** | | **28 / 28** |

판정 분포: 구현 20 · 부분 구현 1(3-3-d) · 보류 4(3-2-b, 3-4-c, E3, E8) · 스코프 밖 3(E1, E2, E9).
