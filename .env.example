# Neon PostgreSQL 연결 문자열. scripts/migrate.ts 와 런타임 DB 클라이언트가 사용한다.
# SvelteKit 코드에서는 $env/dynamic/private 로만 접근하고 process.env 를 직접 읽지 않는다.
DATABASE_URL=

# Vercel Blob 읽기/쓰기 토큰 (M3 업로드 저장 경로에서 사용).
BLOB_READ_WRITE_TOKEN=
